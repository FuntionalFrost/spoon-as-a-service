export default defineAppConfig({
  ui: {
    colors: {
      primary: 'amber', // Matches the 🥄 spoon branding across all default UI elements
      neutral: 'slate' // Cool-toned dark mode
    }
  }
})
